const em = {
  x: 0,
  y: 0,
  speedX: 0,
  initDirection: 'right', // right, left
  shootSpeed: 0,
  canKill: true
};

const robot = {
  x: 0,
  y: 0,
  speedX: 0,
  initDirection: 'right', // right, left
  shootInterval: 0, // in msec
  shootSpeed: 0, // the same for all robots
  canKill: true
};
